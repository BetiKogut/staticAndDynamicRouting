These files contains simple rules for forwardig packets 4th port. 
They are used if incoming packet has ipv4.protocol = OSPF.
They should be uploaded using bash script ==> ../install_cp_rules.sh or directly via Simple_Switch_CLI
